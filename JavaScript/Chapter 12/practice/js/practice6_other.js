document.querySelector("button").addEventListener("dblclick", function(){
  alert("It is the WRONG ACESS!!!");
})

/* 이베트 등록 대상 노드를 선택하고 addEventListener() 매서드에 이벤트 타입과 
그에 따른 실행 함수를 매개변수로 전달해 이벤트를 등록하면 됨 */